# Optional print/export component dependencies (see DESIGN sections 3 and 9)
# Install this file for development; release builds package these separately.
python-docx==1.2.0   # Word output
pypdf==6.14.2        # PDF merging
Pillow==12.3.0       # Payment image loading and conversion
reportlab==5.0.0     # PDF image layout and annotations
