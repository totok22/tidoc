# tidoc core dependencies (required and lightweight; see DESIGN section 3)
pywebview==6.2.1   # Native window using the system WebView
pypdf==6.14.2      # PDF text extraction

# Standard-library modules need no installation: xml, sqlite3, hashlib, hmac, zipfile

# Heavy dependencies are kept in the optional print/export component.
