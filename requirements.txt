# tidoc 核心依赖（必装，全部轻量）——设计文档第 3 节
pywebview==6.2.1   # 原生窗口，用系统自带 WebView，不打包浏览器内核
pypdf==6.14.2      # 读 PDF 文本

# 以下为标准库，无需安装：xml、sqlite3、hashlib、hmac、zipfile

# 重依赖（python-docx / openpyxl / pikepdf / Pillow / reportlab）全部下沉到
# 「打印导出组件」，按需从 COS 下载，不进核心包。
